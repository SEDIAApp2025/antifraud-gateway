{
  "name": "antifraud-gateway",
  "version": "2.1.2",
  "scripts": {
    "gen:metadata": "node scripts/update-metadata.js",
    "version": "npm run gen:metadata && git add src/config/index.ts",
    "dev": "npm run gen:metadata && wrangler dev",
    "deploy": "npm run gen:metadata && wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251209.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@cloudflare/itty-router-openapi": "^1.1.1",
    "itty-router": "^5.0.22"
  }
}
